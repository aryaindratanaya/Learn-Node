const num1 = 5;
const num2 = 10;

function addValues() {
  console.log(`The sum is ${num1 + num2}`);
}

// see that this function is triggered when this file is imported with no values assigned
addValues();
